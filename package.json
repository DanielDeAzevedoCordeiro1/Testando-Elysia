{
  "name": "events-api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --compile --outfile build/events-api",
    "start": "cd build && ./events-api"
  },
  "dependencies": {
    "@elysiajs/openapi": "^1.4.13",
    "amqplib": "^0.10.9",
    "elysia": "latest",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
